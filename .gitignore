node_modules
.DS_Store
*.log
dist
build
uploads/
.env

# Generated by npm run seed
scripts/seed/LAST_RUN_REPORT.md

# Postman desktop app writes its folder-tree sync format into any directory it opens
# (this one held the exported collection). Never commit it. Note: spaces in a .gitignore
# pattern need no quoting/escaping — only trailing spaces do.
outputs/.postman/
outputs/postman/
outputs/LUME Backend API/
# Postman recreates the same sync folders next to the tracked collection file
collection/.postman/
collection/postman/
